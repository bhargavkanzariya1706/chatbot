import React from "react";
import ReactDOM from "react-dom/client";
import "./ChetBot/Main.css";
import Chatbotfile from "./ChetBot/Chatbotfile";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Chatbotfile />
  </React.StrictMode>
);
